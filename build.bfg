# -*- python -*-

main = executable(
    'bt',
    files=find_files('bullet', '*.cc', extra='*.h'),
    compile_options=(
      ['-std=c++1z', '-Wall', '-Werror', '-Wextra', '-pedantic', '-lstdc++fs']),
    link_options=['-lstdc++fs'])
